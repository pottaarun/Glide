/**
 * Declaration stub for the side-effect stylesheet import `import "./index.css"`
 * in `main.tsx`. With `allowArbitraryExtensions` enabled, `tsc` resolves the
 * `.css` import to this file (matched by the `index.d.css.ts` naming pattern),
 * so `npm run check` passes. Vite handles the real CSS at build time — this
 * declaration carries no runtime code.
 */
export {};
